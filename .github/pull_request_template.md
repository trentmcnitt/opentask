## Summary

[What changed and why]

## Test plan

- [ ] Quick check passes (`npm run type-check && npm run lint && npm test`)
- [ ] Integration tests pass (if API/core changes)
- [ ] E2E tests pass (if UI changes)
- [ ] Screenshots attached (if UI changes)
