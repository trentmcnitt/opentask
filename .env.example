# OpenTask Environment Configuration
# Copy this file to .env.local and fill in your values

# NextAuth Configuration
# Generate with: openssl rand -base64 32
AUTH_SECRET=your-secret-key-here

# Optional: Custom database path (defaults to ./data/tasks.db)
# OPENTASK_DB_PATH=/path/to/tasks.db

# Optional: Base URL for the app (used by NextAuth)
# AUTH_URL=http://localhost:3000

# Optional: Data retention periods (all in days)
# Undo log retention (default: 30 days)
# OPENTASK_RETENTION_UNDO_DAYS=30

# Trash retention (default: 30 days)
# OPENTASK_RETENTION_TRASH_DAYS=30

# Completions retention (default: 30 days)
# Per-task stats survive beyond this since they're captured at completion time
# OPENTASK_RETENTION_COMPLETIONS_DAYS=30

# Daily stats retention (default: 365 days)
# OPENTASK_RETENTION_STATS_DAYS=365

# AI Features (requires Claude Code installed and authenticated on server)
# OPENTASK_AI_ENABLED=false

# Per-feature model selection (default: haiku for all)
# OPENTASK_AI_ENRICHMENT_MODEL=haiku
# OPENTASK_AI_BUBBLE_MODEL=haiku
# OPENTASK_AI_BRIEFING_MODEL=haiku
# OPENTASK_AI_TRIAGE_MODEL=haiku
# OPENTASK_AI_SHOPPING_MODEL=haiku

# Max queries per warm enrichment subprocess before recycling (default: 8)
# OPENTASK_AI_MAX_REUSES=8

# SDK query timeout in ms (default: 60000)
# OPENTASK_AI_QUERY_TIMEOUT_MS=60000

# Max concurrent SDK subprocesses (default: 2)
# OPENTASK_AI_MAX_CONCURRENT=2

# Max wait time for a semaphore slot in ms (default: 30000)
# OPENTASK_AI_QUEUE_TIMEOUT_MS=30000

# Path to Claude Code CLI (only needed in standalone/bundled builds)
# OPENTASK_AI_CLI_PATH=/home/admin/.npm-global/lib/node_modules/@anthropic-ai/claude-code/cli.js

# AI activity log retention in days (default: 90)
# OPENTASK_RETENTION_AI_ACTIVITY_DAYS=90
